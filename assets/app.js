const products=[{id:1,name:'تفاح أحمر',detail:'مختار بعناية',price:75,icon:'🍎',tone:'#f3dfd8'},{id:2,name:'موز بلدي',detail:'حلو وطبيعي',price:48,icon:'🍌',tone:'#f7edc8'},{id:3,name:'طماطم',detail:'حصاد اليوم',price:32,icon:'🍅',tone:'#f4ded2'},{id:4,name:'فستق حلبي',detail:'درجة أولى',price:260,icon:'🥜',tone:'#dfeada'}];
const orders=[{id:'#1048',name:'أحمد محمود',area:'المنطقة الأولى',total:'1,240 ل.ت',time:'10:00 - 12:00'},{id:'#1047',name:'سارة علي',area:'المنطقة الثانية',total:'680 ل.ت',time:'في أقرب وقت'},{id:'#1045',name:'محمود سمير',area:'المنطقة الثالثة',total:'890 ل.ت',time:'14:00 - 16:00'}];
let cart={};
const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);
function showView(view){$$('.view').forEach(v=>v.classList.remove('active-view'));$('#'+view).classList.add('active-view');$$('[data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===view));window.scrollTo(0,0);history.replaceState(null,'','#'+view)}
$$('[data-view]').forEach(b=>b.addEventListener('click',()=>showView(b.dataset.view)));
function renderProducts(){ $('#productGrid').innerHTML=products.map(p=>`<article class="product-card"><div class="product-img" style="--tone:${p.tone}">${p.icon}</div><div class="product-info"><small>${p.detail}</small><h3>${p.name}</h3><div class="buy-row"><div class="price"><b>${p.price}</b> ل.ت <small>/ كجم</small></div><button class="add" onclick="add(${p.id})">+ أضف</button></div></div></article>`).join('') }
function add(id){cart[id]=(cart[id]||0)+1;renderCart();openCart()}
function removeOne(id){cart[id]--;if(cart[id]<=0)delete cart[id];renderCart()}
function renderCart(){const count=Object.values(cart).reduce((a,b)=>a+b,0);$('#cartCount').textContent=count;const chosen=products.filter(p=>cart[p.id]);$('#cartLines').innerHTML=chosen.length?chosen.map(p=>`<div class="cart-line"><span>${p.icon}</span><div><b>${p.name}</b><small>${cart[p.id]} × ${p.price} ل.ت</small></div><button onclick="removeOne(${p.id})">−</button></div>`).join(''):'<div class="empty-cart">السلة فارغة حاليًا</div>';$('#cartTotal').textContent=chosen.reduce((s,p)=>s+p.price*cart[p.id],0).toLocaleString('ar')+' ل.ت'}
function openCart(){$('#cartDrawer').classList.add('open');$('#overlay').classList.add('open')};function closeCart(){$('#cartDrawer').classList.remove('open');$('#overlay').classList.remove('open')}
$('#openCart').addEventListener('click',openCart);$('#closeCart').addEventListener('click',closeCart);$('#overlay').addEventListener('click',closeCart);
$('#driverOrders').innerHTML=orders.map(o=>`<article class="order"><div><b>${o.id} — ${o.name}</b><small>${o.area} • ${o.time}</small></div><div><b>${o.total}</b><small>الدفع عند الاستلام</small></div><div class="order-actions"><button class="details-button">التفاصيل</button><button class="done-button">تم التسليم</button></div></article>`).join('');
renderProducts();renderCart();const initial=location.hash.slice(1);if(['store','driver','admin'].includes(initial))showView(initial);
